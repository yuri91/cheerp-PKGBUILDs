# Maintainer: Yuri Iozzelli <y.iozzelli@gmail.com>
pkgname=cheerp-libcxx
pkgver=10.0ece355
pkgrel=1
epoch=
pkgdesc="A C++ compiler for the Web, C++ library implementation"
arch=("x86_64")
url="leaningtech.com/cheerp"
license=('GPL2')
groups=()
depends=("cheerp-llvm-clang" "cheerp-utils" "cheerp-newlib")
makedepends=()
checkdepends=()
optdepends=()
provides=()
conflicts=()
replaces=()
backup=()
options=('!strip')
install=
changelog=
source=("git+ssh://git@bitbucket.org/apignotti/cheerp-libcxx.git"
"git+ssh://git@bitbucket.org/apignotti/cheerp-libcxxabi.git")
md5sums=('SKIP'
         'SKIP')

pkgver() {
  cd "$_pkgname"
  echo $(git rev-list --count HEAD).$(git rev-parse --short HEAD)
}

prepare_mode() {
	cd $srcdir/cheerp-libcxx
	mkdir -p build_$1
	cd build_$1
	cmake -DCMAKE_INSTALL_PREFIX=/opt/cheerp \
	      -DCMAKE_BUILD_TYPE=Release \
	      -DCMAKE_TOOLCHAIN_FILE=/opt/cheerp/share/cmake/Modules/$1Toolchain.cmake \
	      -DCHEERP_LINEAR_OUTPUT=asmjs \
	      -DLIBCXX_ENABLE_SHARED=OFF \
	      -DLIBCXX_ENABLE_ASSERTIONS=OFF \
	      -DLIBCXX_CXX_ABI_INCLUDE_PATHS=$srcdir/cheerp-libcxxabi/include \
	      -DLIBCXX_CXX_ABI=libcxxabi \
	      -DCMAKE_CXX_FLAGS="-nostdlib -frtti" ..
}
prepare() {
	prepare_mode Cheerp
	prepare_mode CheerpWasm
}

build() {
	pushd $srcdir/cheerp-libcxx/build_Cheerp
	make
	popd
	pushd $srcdir/cheerp-libcxx/build_CheerpWasm
	make
	popd
}

package() {
	pushd $srcdir/cheerp-libcxx/build_Cheerp
	make DESTDIR=$pkgdir install
	popd
	pushd $srcdir/cheerp-libcxx/build_CheerpWasm
	make DESTDIR=$pkgdir install
	popd

}
